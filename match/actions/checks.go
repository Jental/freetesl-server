package actions

import (
	"github.com/jental/freetesl-server/models"
)

func isGuardPresentInLane(playerState *models.PlayerMatchState2, laneID byte) bool {
	// var lane []*models.CardInstance
	// if laneID == common.LEFT_LANE_ID {
	// 	lane = playerState.LeftLaneCards
	// } else if laneID == common.RIGHT_LANE_ID {
	// 	lane = playerState.RightLaneCards
	// } else {
	// 	return false
	// }

	// slices.IndexFunc(lane, func(c *model.CaredInstance) bool { return c. })
	return false
}
